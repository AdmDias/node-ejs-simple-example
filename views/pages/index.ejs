<!DOCTYPE html>
<html>
    <%- include('../partials/head') %>

    <body class="container">
        <%- include('../partials/header') %>
        <main>
            <div class="jumbotron">
                <h1>Node with EJS</h1>
                <p>Learning how to work with EJS</p>
                <p><%= subtitle %></p>
                <br/>
                <p>Simple example below using foreach to render a dynamic list.</p>
                <ul>
                    <% qualitys.forEach(({ title, message }) => { %>
                        <li>
                            <strong><%= title %></strong><%= message %>
                        </li>
                    <% }) %>
                </ul>
            </div>
        </main>
        <%- include('../partials/footer') %>
    </body>
</html>